#ifndef FIRST_PASS_H
#define FIRST_PASS_H
#include "constants.h"
#include "data_structures.h"

#define INITIAL_IC 100

bool process_first_pass(head*, char*);
int label_check(char*, char*);
operation firstWord(char*);

#endif